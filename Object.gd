extends Node2D


@export var type : String
var i : int
var j : int
var prev_i : int
var prev_j : int

@export var vi : int
@export var vj : int

var id : int


# Called when the node enters the scene tree for the first time.
func _ready():
	pass
	

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	#$Label.text = str(i) + " " + str(j) + " fdsaea"
	pass
